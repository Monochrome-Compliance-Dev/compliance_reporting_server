# Mappings

Defines how raw dataset fields map to canonical fields.

Owns:

- Field mapping definitions
- Column mapping logic
- Mapping governance rules
- Source field prioritisation

Does NOT own:

- Raw ingestion
- Canonical record storage
- Business state decisions

Notes:
Mappings define meaning, not outcomes.
