# Shared

Reusable components across domains.

Owns:

- Cross-domain utilities
- Shared validation logic
- Reusable helpers

Does NOT own:

- Domain-specific logic

Notes:
Use sparingly. Prefer domain ownership where possible.
